<h1>Patient</h1>
<div class = "tile-container">
  <% @patients.each do |patient| %>
  <%= @patient.name %>
  <%= @patient.age %>
  <%= @patient.sex %>
  <%= @patient.diagnosis %>
  <%= @patient.mealplan %>


  <% if current_doctor == @doctor.user %>
  <p><%= link_to 'Add', new_patient_path(@patient),%>|<%= link_to 'Edit', edit_patient_path(@patient),%> | <%= link_to 'Delete',patient_path(@patient), method: :delete, data: {confirm: 'Are you sure you want to delete this patient?'} %></p>

<p><%= link_to 'All Patients', patient_path(@patient)%> </p>

<h1>Search for new receipts,
receipts API </h1>


<%end%>


</div>
