class Recipe < ApplicationRecord

  # scope :name, -> (name) { where("name like ?","%#{name}%") }
  # scope :url, -> (url) { where url: url }
  # scope :health_label, -> (health_label) { where("health_label like ?","%#{health_label}%") }
  # scope :ingredients, -> (ingredients) { where("ingredients like ?","%#{ingredients}%") }
  # scope :calories, -> (calories) { where("calories like ?", "%#{calories}%") }

end
